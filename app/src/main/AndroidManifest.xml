<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.francisco.cafuture_v2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cafuture_v2">
        <activity android:name=".ui.carrito"></activity>
        <activity android:name=".ui.menu_comida_italiana" />
        <activity android:name=".ui.menu_comida_mexicana" />
        <activity android:name=".ui.menu_postres" />
        <activity
            android:name=".ui.Postres"
            android:label="@string/Postres"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".ui.PostresAdapter"
            android:label="@string/PostresAdapter"
            tools:ignore="Instantiatable" />
        <activity android:name=".RegistryJavaActivity" />
        <activity
            android:name=".HomeActivity"
            android:label="@string/title_activity_segundo"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.Cafuture_v2.NoActionBar" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>